def reverse_string(phrase):
    """Reverse string,

        >>> reverse_string('awesome')
        'emosewa'

        >>> reverse_string('sauce')
        'ecuas'
    """
    return phrase[::-1]
    # new_str = ''
    # index = len(phrase) - 1
    # while index >= 0:
    #     new_str += phrase[index]
    #     index -= 1
    # return new_str
    
